[project]
name = "uconvert"
version = "0.1.0"
description = "CLI converter"
requires-python = ">=3.10"
dependencies = [
    "pillow>=10.0.0",
    "pypdf>=4.0.0",
    "pywin32>=306; platform_system == 'Windows'"
]

[project.scripts]
uconvert = "uconvert.cli:main"